{
  "scripts": {
    "dev": "concurrently \"live-server src\" \"nodemon\"",
    "build": "npx tailwindcss -i ./src/assets/styles.css -o ./src/build-styles.css --minify"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9",
    "tailwindcss": "^3.4.10",
    "vite": "^6.2.2"
  }
}
